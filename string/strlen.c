#include <stdio.h>
#include <string.h>

int main( )
{
    int len;
    char array[20]="bhagyshreelovespavan" ;

    len = strlen(array) ;

    printf ( "\string length  = %d \n" , len ) ;
    return 0;
}
